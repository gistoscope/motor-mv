# @motor/engine-service

Skeleton clients to talk to a remote Engine over HTTP or WebSocket.
- `makeHttpPort(baseUrl)`: POST /intent.
- `makeWsPort(url)`: send/receive JSON messages with the same `id`.
