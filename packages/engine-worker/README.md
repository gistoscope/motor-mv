# @motor/engine-worker

Skeleton for a WebWorker-based Engine transport.

- `port.worker.ts`: create a `port` for main thread (Viewer side).
- `worker.entry.ts`: worker entry (Engine side). Replace `handleIntent` with real logic.
